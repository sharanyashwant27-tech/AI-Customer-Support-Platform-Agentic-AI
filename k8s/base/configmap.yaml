apiVersion: v1
kind: ConfigMap
metadata:
  name: aics-config
data:
  ENVIRONMENT: "production"
  HOST: "0.0.0.0"
  PORT: "8917"
  VECTOR_STORE: "qdrant"
  DEFAULT_LLM_PROVIDER: "openai"
  LOG_FORMAT: "json"
  ENABLE_METRICS: "true"
  API_PREFIX: "/api/v1"
